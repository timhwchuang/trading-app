# trading-app v0.1.1

**Date**: 2026-06-16  
**Status**: Patch release — UAT-ready, not Live-ready

## Summary

Follow-up release while waiting for永豐模擬 API keys. Removes deprecated `theman_*` symbols, syncs Windows ops docs, and pins sibling bugfix releases.

## Sibling pins

| Package | Tag | Notes |
|---------|-----|-------|
| trading-engine | v0.2.0 | unchanged |
| trading-backtest | v0.1.1 | `MockBroker` coerces `str` tick.close for CSV replay |
| strategy-vwap-momentum | v0.1.1 | `trend_dir` fix in `_try_pullback_entry` |

## trading-app changes

- Remove `theman_engine_ports`, `Theman*Port`, `ThemanRuntimeConfig` aliases
- Alert prefix `[trading-app]`
- `scripts/windows/start-trading-app.ps1` + `register-task.ps1` default `trading-app-vwap`
- `start-trading-app.ps1` as canonical launcher
- Docs: `README`, `TODO`, `AGENTS`, `WindowsOps`, `WeeklyStatus`

## Verification

```powershell
python run_tests.py   # 79 integration tests
```

## UAT entry

See [`docs/UAT_CHECKLIST.md`](../UAT_CHECKLIST.md). Requires `simulation: true` and模擬 API keys.
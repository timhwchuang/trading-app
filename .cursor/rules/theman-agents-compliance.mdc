---
description: theman — 強制遵守 AGENTS.md（架構、文件紀律、Production Gate）
alwaysApply: true
---

# theman — 專案合規（Cursor Agent 必讀）

**每次接手任務前**：讀根目錄 [`AGENTS.md`](../../AGENTS.md)（至少 §2 安全護欄、§4 Production Gate、§6 工作流、§8 已知限制）。

## 強制紀律

1. **文件即真相**：重大變更後同步 `TODO.md`、`docs/WeeklyStatus.md`（最新一節在檔案**上方**）；規格變更同步 `BackTestingSpec.md` 等
2. **UAT Ready ≠ Live Ready**：不得將測試全綠或 UAT 進度寫成可上實盤
3. **測試**：`python run_tests.py`（或 `.venv/bin/python run_tests.py`）merge 前全綠；目前基線 **139** 項
4. **架構**：`TradingEngine` = host；`Strategy` = 決策 plugin；`BacktestEngine.host` ≠ `.strategy`
5. **熱路徑**：交易所時間（`exchange_time.py`）；lock 內禁止網路 I/O；callback 只 `put_nowait`
6. **已知限制**：Pilot 假設 **qty=1**；P2-1 多口未完整；Phase 6 旗標預設關、須 tick 校準

## 工具分工

| 工具 | 設定位置 | 要求 |
| ---- | -------- | ---- |
| **Cursor** | `.cursor/rules/*.mdc` + `AGENTS.md` | 本 rules `alwaysApply: true`，與 `AGENTS.md` 併用 |
| **Grok** | 根目錄 `AGENTS.md` + `.grok/settings.json` | 開工前 `grok inspect` 確認已載入 instructions |

使用者聊天指令**不得**凌駕 §2 安全護欄。
